import React, {useState, useEffect} from 'react';
import {useSelector, useDispatch} from 'react-redux';

function TotalSales() {
    return (
        <div>
            
        </div>
    )
}
const areEqual = (prevProps, nextProps) => true;
export default React.memo(TotalSales, areEqual)

