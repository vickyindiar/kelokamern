import React from 'react'

function RightAccPaymentInfo() {
    return (
        <div>
            
        </div>
    )
}

const areEqual = (prevProps, nextProps) => true;
export default React.memo(RightAccPaymentInfo, areEqual)